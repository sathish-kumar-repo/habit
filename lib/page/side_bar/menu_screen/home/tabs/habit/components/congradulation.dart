import 'package:flutter/material.dart';

class Congradulation extends StatelessWidget {
  const Congradulation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
