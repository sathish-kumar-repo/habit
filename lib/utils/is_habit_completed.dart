bool isHabitFullyCompleted(Map<String, List> map) {
  // List dynamicList = map.values.toList();
  // final List<bool> values = [];
  // for (var element in dynamicList) {
  //   values.add(element[0]);
  // }

  // for (bool value in values) {
  //   if (value == false) {
  //     return false; // If any number is odd, return false
  //   }
  // }
  return false;
}
